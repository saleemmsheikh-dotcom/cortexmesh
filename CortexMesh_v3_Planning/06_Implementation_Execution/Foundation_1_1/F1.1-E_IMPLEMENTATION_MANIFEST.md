# Foundation 1.1-E Implementation Manifest

## Manifest control

| Field | Value |
| --- | --- |
| Manifest | Foundation 1.1-E Repository-First Environment Contract |
| Version | 1.1 |
| Status | ACCEPTED — IMPLEMENTATION AUTHORIZED |
| Date | 2026-07-17 |
| Repository baseline | `dace93dc68c1f8b793456da6f19a66bda4e2a87d` |
| Required branch | `foundation-1.1e-environment-contract` |
| Architecture | Repository-first |
| Automatic merge | PROHIBITED |

This manifest is immutable as accepted. Any path, dependency, pin,
command, test, work-unit, or ordering change requires a new manifest version
and Product Owner review before execution continues.

## Revision history

| Version | Disposition | Change |
| --- | --- | --- |
| 1.0 | Superseded procedurally | Original execution order required Ubuntu CP2 evidence before branch publication |
| 1.1 | Accepted | Permits one early feature-branch publication solely for CP2 Ubuntu dependency-profile validation |

Version 1.1 changes execution procedure only. It does not change IM001–IM012,
dependency versions, W1–W4 membership, technical scope, protected surfaces,
tests, pins, or acceptance criteria.

## Accepted planning identities

| Artifact | SHA-256 |
| --- | --- |
| `F1.1-E_CURRENT_STATE_ASSESSMENT.md` | `43e496f2ae545a417a225788974b9045d8d4a3b5854a11ff2ec23419a511229b` |
| `F1.1-E_GAP_ANALYSIS.md` | `c59c12b83f715f92d801edb0f01d6292a36f20247d357df4337bb84ee9077327` |
| `F1.1-E_PACKAGING_ENVIRONMENT_DESIGN.md` | `28962590018c7211991341f2496a359a0678e14482c0722774a4471b367d6e56` |
| `F1.1-E_EXECUTION_PLAN.md` | `c068e36a669a663efdf62a6aad576c0c4a264648b375b3e152ea506a8a7825d6` |
| `F1.1-E_ACCEPTANCE_CRITERIA.md` | `1f4a254d56716701f3b2ad3091ce3df6efc5f8c3437a6abfeb029c0ea9d5870d` |

All five artifacts are immutable during implementation.

## Protection classes

| Class | Meaning |
| --- | --- |
| P0 | Protected runtime, governance authority, research, validation, evidence, or LOCKED surface; no modification authorized |
| P1 | Accepted planning/evidence/status record; only exact manifest action authorized |
| P2 | Repository documentation, dependency, CI, or test control within the manifest |
| P3 | Disposable generated material; none may be tracked |

## Authorized implementation register

| ID | Path | Action | Class | Expected result | Verification |
| --- | --- | --- | --- | --- | --- |
| IM001 | `README.md` | Update | P2 | Accurate CPython 3.14/Ubuntu CI claim; concise link to environment contract; stateful CLI warning before command | DOC001–DOC008 |
| IM002 | `ENVIRONMENT.md` | Create | P2 | Authoritative repository-first environment contract | DOC001–DOC018 |
| IM003 | `requirements.txt` | Update | P2 | Default verification/development direct dependencies only | DEP001–DEP008 |
| IM004 | `requirements-openai.txt` | Create | P2 | Combined optional OpenAI profile with unambiguous inclusion of default profile | DEP001–DEP010 |
| IM005 | `constraints/cpython314-ubuntu.txt` | Create | P2 | Exact verified-profile constraints bound to published Ubuntu resolution | DEP011–DEP020 |
| IM006 | `.github/workflows/quality-gates.yml` | Update install/cache inputs only | P2 control | Regression installs under IM005 constraints; all other F1.1-D semantics and pins unchanged | CI001–CI012 |
| IM007 | `.github/workflows/quality-observations.yml` | Update install/cache inputs only | P2 control | Coverage/audit inputs install under IM005 constraints; all other semantics and pins unchanged | CI001–CI014 |
| IM008 | `tests/test_f11e_environment_contract.py` | Create | P2 | Static and functional environment-contract controls | T001–T024 |
| IM009 | `CortexMesh_v3_Planning/06_Implementation_Execution/Foundation_1_1/F1.1-E_VERIFICATION_EVIDENCE.md` | Create after real runs | P1 | Factual implementation, clean-profile, protected-identity, artifact, and CI evidence | EVID001–EVID012 |
| IM010 | `CortexMesh_v3_Planning/06_Implementation_Execution/Foundation_1_1/FOUNDATION_1_1_STATUS.md` | Status-only update after validation | P1 | Accurate acceptance-pending state | REC001–REC004 |
| IM011 | `CortexMesh_v3_Planning/06_Implementation_Execution/IMPLEMENTATION_EXECUTION_TRACKER.md` | Status-only update after validation | P1 | Accurate execution state | REC001–REC004 |
| IM012 | `CortexMesh_v3_Planning/05_Program_Management/PROGRAM_ROADMAP.md` | Status-only update after validation | P1 | Accurate program state | REC001–REC004 |

No other path is authorized. Directory creation is limited to the parent
directory required for IM005.

## Exact content contracts

### IM001 support and onboarding contract

README must:

- replace the generic Python 3 badge/claim with CPython 3.14 / Ubuntu CI
  verified language;
- distinguish verified from observed and uncertified environments;
- keep clone and repository-root entry instructions concise;
- link `ENVIRONMENT.md` as authoritative;
- retain the canonical unittest command;
- place an explicit `memory/memory.json` mutation warning before the CLI
  command;
- state that optional provider/capability paths require separate
  configuration;
- make no installable-package or broad-platform claim.

### IM002 required structure

`ENVIRONMENT.md` must contain, in this order:

1. contract status and vocabulary;
2. verified/observed/not-certified environment table;
3. repository-root requirement;
4. dependency profile table;
5. verification-first fresh-clone procedure;
6. stateful CLI contract;
7. optional OpenAI profile;
8. optional Local AI profile;
9. Docker external-capability limitation;
10. complete environment-variable reference;
11. dependency update and rollback policy;
12. packaging deferral threshold;
13. unsupported/non-certified capabilities.

Historical evidence may be linked. It must not be edited or restated as a new
certification.

### IM003 exact direct declarations

`requirements.txt` must contain comments identifying it as the default
verification/development profile and these two non-comment declarations only:

```text
pytest>=7.0.0
coverage>=7.0.0
```

Order must be exactly as shown.

### IM004 exact optional profile

`requirements-openai.txt` must contain role comments and these non-comment
lines only:

```text
-r requirements.txt
openai>=1.0.0
```

This fixes the optional OpenAI profile as the default verification/development
profile plus the SDK. It does not imply that the default runtime needs OpenAI.

### IM005 constraints provenance

The initial constraints content is bound to successful GitHub run
`29565319116`:

| Field | Evidence |
| --- | --- |
| Workflow | Quality Gates |
| Head | `dace93dc68c1f8b793456da6f19a66bda4e2a87d` |
| Runner | GitHub `ubuntu-latest` |
| Python | CPython 3.14.6 |
| pip | 26.1.2 |
| Input requirements SHA-256 | `5dc411c7029179a275a6048c61047224c28bca4349fc8710472ef71d4d06d53b` |
| Result | 250/250 PASS |

The constraints file must contain provenance comments and exactly these
case-normalized requirement entries, sorted by normalized project name:

```text
annotated-types==0.7.0
anyio==4.14.2
certifi==2026.6.17
coverage==7.15.2
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
jiter==0.16.0
openai==2.46.0
packaging==26.2
pluggy==1.6.0
pydantic==2.13.4
pydantic-core==2.46.4
pygments==2.20.0
pytest==9.1.1
sniffio==1.3.1
tqdm==4.68.4
typing-extensions==4.16.0
typing-inspection==0.4.2
```

Underscore forms emitted by `pip freeze` are normalized to canonical
hyphenated project names. This is a textual normalization only; versions are
unchanged.

The implementation must validate both profiles under this constraints file in
a clean CPython 3.14 Ubuntu environment. If either fails, stop. Do not alter a
version during execution.

The file must not contain:

- hashes or claims of hash-locking;
- editable installs;
- local paths;
- direct URLs;
- index URLs;
- credentials;
- environment markers not present in the recorded evidence;
- pip or setuptools as application constraints.

### IM006–IM007 workflow edit boundary

Only these workflow behaviors may change:

- cache dependency paths may list `requirements.txt`,
  `requirements-openai.txt`, and `constraints/cpython314-ubuntu.txt` as
  appropriate;
- every `pip install -r requirements.txt` becomes:

```text
python -m pip install \
  -c constraints/cpython314-ubuntu.txt \
  -r requirements.txt
```

- dependency audit continues to audit the direct default profile and may use
  the constraints file only in installation, not as a claim that transitives
  are direct dependencies;
- environment records add hashes for IM003–IM005.

No optional OpenAI profile installation or API request is added to CI by this
manifest. IM004 is validated in the clean-profile checkpoint without
credentials.

The following remain byte-for-byte unchanged except line movement strictly
required by the authorized install block:

- workflow names and triggers;
- permissions and concurrency;
- Python version;
- job names and runner;
- Action references;
- `pip-audit` pin and descriptive policy;
- coverage command and non-threshold policy;
- artifact names, allowlists, and retention;
- protected-path behavior;
- canonical unittest command.

## Immutable supply-chain references

| Pin | Value |
| --- | --- |
| PIN001 `actions/checkout@v4` | `34e114876b0b11c390a56381ad16ebd13914f8d5` |
| PIN002 `actions/setup-python@v6` | `ece7cb06caefa5fff74198d8649806c4678c61a1` |
| PIN003 `actions/upload-artifact@v4` | `ea165f8d65b6e75b540449e92b4886f43607fa02` |
| PIN004 `pip-audit` | `2.10.1` |

Any difference stops execution and requires a new manifest.

## Test register

IM008 must contain at least these independently named controls:

| ID | Required test behavior |
| --- | --- |
| T001 | README links to `ENVIRONMENT.md` |
| T002 | CPython 3.14 and Ubuntu CI are identified as verified |
| T003 | Generic Python 3 compatibility wording is rejected |
| T004 | Observed/uncertified environments are distinguished |
| T005 | Repository root is the supported working directory |
| T006 | Verification-first sequence precedes CLI/provider/Docker execution |
| T007 | Canonical unittest command is exact |
| T008 | Pytest is optional parity, not canonical authority |
| T009 | README state warning precedes CLI command |
| T010 | `memory/memory.json` and backup possibility are disclosed |
| T011 | Destructive reset, deletion, state relocation, or stateless claims are rejected |
| T012 | IM003 non-comment declarations and order are exact |
| T013 | IM004 non-comment declarations and order are exact |
| T014 | IM005 entries and versions are exact, sorted, and duplicate-free |
| T015 | IM005 rejects paths, URLs, credentials, editables, and unexpected markers |
| T016 | Workflow default installs use IM005 |
| T017 | Workflow Python version remains 3.14 |
| T018 | PIN001–PIN004 remain exact |
| T019 | Workflow permissions, jobs, artifacts, and canonical commands remain within F1.1-D contract |
| T020 | All current `CORTEX_LOCAL_AI_*` names are documented |
| T021 | Docker is separate from Local AI and base image is labeled unprovisioned |
| T022 | No credential values or secret-bearing examples appear |
| T023 | No packaging metadata or distribution workflow is introduced |
| T024 | E1–E3 planning hashes remain exact |

Tests must be standard-library `unittest` compatible. They must not import
provider clients, invoke services, execute the live CLI, write memory, or
create tracked artifacts.

## Documentation controls

| ID | Requirement |
| --- | --- |
| DOC001 | One authoritative current environment guide |
| DOC002 | Verified claim matches Ubuntu/CPython 3.14 CI |
| DOC003 | No generic Python/platform compatibility claim |
| DOC004 | Default standard-library runtime distinguished from development dependencies |
| DOC005 | Default and optional OpenAI install profiles separated |
| DOC006 | Verification precedes mutable/external execution |
| DOC007 | CLI mutation and tracked memory disclosed |
| DOC008 | No destructive state recovery instruction |
| DOC009 | OpenAI keys described by name only; no values |
| DOC010 | Missing OpenAI keys documented as fallback condition |
| DOC011 | Local AI variables and service/model ownership accurate |
| DOC012 | No provider preference or selection authority |
| DOC013 | Docker not required for SAFE Local AI bridge |
| DOC014 | Capability base image explicitly not repository-provisioned |
| DOC015 | No fresh-clone Docker capability guarantee |
| DOC016 | Dependency update/rollback policy present |
| DOC017 | Packaging threshold present |
| DOC018 | No production, security, universal compatibility, or package-readiness claim |

## Dependency controls

| ID | Requirement |
| --- | --- |
| DEP001 | IM003 direct declarations exact |
| DEP002 | IM004 direct declarations exact |
| DEP003 | Default profile clean install succeeds under IM005 |
| DEP004 | Optional OpenAI profile clean install succeeds under IM005 |
| DEP005 | `pip check` passes in both clean profiles |
| DEP006 | No API key is set or request made during profile validation |
| DEP007 | Default canonical regression passes in default profile |
| DEP008 | Pytest parity passes in default profile |
| DEP009 | Optional profile can import `openai` without network execution |
| DEP010 | Default profile does not install `openai` |
| DEP011 | Constraints entries exactly match manifest |
| DEP012 | Constraints provenance header accurate |
| DEP013 | Constraints contains no prohibited source |
| DEP014 | Resolution uses CPython 3.14 Ubuntu |
| DEP015 | pip/Python and input hashes recorded |
| DEP016 | Descriptive pip-audit completes without infrastructure failure |
| DEP017 | No dependency automatically upgraded or rewritten |
| DEP018 | Local AI/Docker remain outside pip declarations |
| DEP019 | No lockfile/universal/hash-lock claim introduced |
| DEP020 | Resolution evidence preserved for review |

## CI controls

| ID | Requirement |
| --- | --- |
| CI001 | PIN001–PIN004 exact |
| CI002 | CPython remains 3.14 |
| CI003 | Default installs use IM005 |
| CI004 | Cache dependency paths cover changed dependency inputs |
| CI005 | Canonical unittest command unchanged |
| CI006 | Existing required jobs unchanged |
| CI007 | Existing observational jobs unchanged |
| CI008 | Permissions remain read-only |
| CI009 | No secrets or provider execution |
| CI010 | Coverage remains descriptive |
| CI011 | Dependency audit remains descriptive |
| CI012 | Scope, whitespace, compile, and regression all pass |
| CI013 | Coverage and dependency-audit both pass |
| CI014 | Artifacts remain within existing allowlists |

## Evidence and record controls

| ID | Requirement |
| --- | --- |
| EVID001 | Planning baseline and manifest commit recorded |
| EVID002 | Implementation commits and exact branch recorded |
| EVID003 | Clean default and optional profile logs recorded |
| EVID004 | Focused and full local test results recorded |
| EVID005 | Fresh-clone simulation result recorded |
| EVID006 | Constraints provenance and content hash recorded |
| EVID007 | First pull-request workflow run URLs/IDs recorded |
| EVID008 | Action pins and workflow boundary verified |
| EVID009 | P0/LOCKED and E1–E3 identities exact |
| EVID010 | No packaging metadata or external publication confirmed |
| EVID011 | Rollback verified |
| EVID012 | Product Owner acceptance remains pending until granted |
| REC001 | Status says implementation validated, acceptance pending |
| REC002 | No record claims complete before acceptance/merge |
| REC003 | F1.1-B remains paused and F1.1-C/D remain complete |
| REC004 | F1.1-E next state is accurate across all three records |

## Protected and excluded surfaces

### P0 — no modification

- `main.py`, `orchestrator.py`, and all live runtime source;
- `memory/`, including `memory/memory.json`;
- `agents/`, Local AI bridge/solver behavior, and provider code;
- `core/`, especially `core/external_runner.py`;
- governance records, Board records, and Lock Registry;
- Phase 1B provider implementation and sealed evidence;
- Phase 2C reference orchestration implementation and evidence;
- Phase 3A replay, validation, certification, and evidence;
- `CortexMesh_v3_Planning/07_Research/`, including RP-001;
- Foundation baseline and prior closeout evidence;
- licensing and legal records.

### Excluded technical actions

- package metadata or build backend;
- import/source layout changes;
- state relocation/reset/initialization changes;
- Dockerfile, Compose, image, registry, or provisioning script;
- provider install, invocation, selection, or request;
- new platform certification;
- dependency beyond IM003–IM005;
- GitHub settings, secrets, or branch protection;
- deployment, release, or publication.

## Prohibited action register

| ID | Prohibited action |
| --- | --- |
| X001 | Edit any unlisted path |
| X002 | Modify E1–E3 planning artifacts after manifest acceptance; this does not prohibit the separately authorized v1.0-to-v1.1 manifest-only revision |
| X003 | Add `pyproject.toml`, `setup.py`, `setup.cfg`, package build, wheel, or console script |
| X004 | Modify runtime, imports, memory behavior, Local AI, providers, Docker runner, governance, research, validation, or LOCKED files |
| X005 | Change PIN001–PIN004 |
| X006 | Add secrets, credential values, `.env` loading, or secret-dependent CI |
| X007 | Invoke OpenAI, Ollama, LM Studio, Docker, or external capability during default verification |
| X008 | Change provider selection, authority, ranking, scoring, or fallback behavior |
| X009 | Claim Windows, generic Python, universal, production, security, package, or external-capability certification |
| X010 | Generate a universal/hash lock without new design |
| X011 | Upgrade or substitute a manifest dependency version |
| X012 | Add opportunistic cleanup, formatting, or unrelated documentation |
| X013 | Modify GitHub administrative settings |
| X014 | Merge automatically or before Product Owner acceptance |
| X015 | Rewrite or squash approved history unless separately authorized |

## Immutable execution order

Execution must occur in this exact order:

1. commit and publish the accepted E1–E3 planning package and this manifest to
   `main`;
2. restart all pre-execution gates from G001;
3. create `foundation-1.1e-environment-contract`;
4. execute W1;
5. complete checkpoint CP1;
6. complete the W2 candidate scope and its static dependency tests;
7. create a provisional W2 commit containing only the exact W2 membership;
8. publish the feature branch early solely for CP2 Ubuntu dependency
   validation;
9. independently validate the default and optional OpenAI profiles in fresh
   CPython 3.14 Ubuntu environments using the manifest commands;
10. if validation passes, retain the provisional commit unchanged as the
    official W2 commit and complete CP2;
11. if validation fails, stop, preserve evidence, and correct only W2 scope
    under separate review of the failure;
12. execute W3;
13. complete checkpoint CP3 and full local verification;
14. publish the updated branch and open a pull request;
15. inspect first unmodified pull-request CI runs;
16. execute W4 only from factual evidence;
17. complete checkpoint CP4;
18. request Product Owner acceptance;
19. do not merge until explicitly authorized;
20. after authorized merge, complete post-merge checkpoint CP5;
21. record completion separately.

No step may be skipped or reordered.

### Early-publication boundary

The early publication at step 8:

- is limited to W1, the manifest v1.1 procedural commit, and the provisional W2
  commit;
- does not authorize W3, a pull-request approval, merge, release, tag,
  deployment, or external publication;
- must not change tests, dependency versions, or immutable references;
- must use a fresh Ubuntu / CPython 3.14 environment;
- must validate these profiles independently:

```text
python -m pip install \
  -r requirements.txt \
  -c constraints/cpython314-ubuntu.txt

python -m pip install \
  -r requirements-openai.txt \
  -c constraints/cpython314-ubuntu.txt
```

The validation mechanism itself must already be authorized and capable of
executing both commands. If no such Ubuntu mechanism exists without adding an
unlisted file or changing W3 early, stop before publishing the provisional W2
commit and request review.

## Atomic work units

| Unit | Exact membership | Commit boundary |
| --- | --- | --- |
| W1 | IM001, IM002, documentation-focused portion of IM008 | One commit: `Define CortexMesh repository environment contract` |
| W2 | IM003, IM004, IM005, dependency-focused portion of IM008 | One commit: `Define verified dependency profiles` |
| W3 | IM006, IM007, CI-focused portion of IM008 | One commit: `Apply verified constraints in CI` |
| W4 | IM009–IM012 | One commit: `Record Foundation 1.1-E implementation evidence` |

IM008 evolves only by adding the tests assigned to W1–W3. Existing tests may
not be weakened, renamed, or removed.

## Pre-execution gates

| Gate | Requirement |
| --- | --- |
| G001 | Product Owner accepts E3 design and manifest version 1.1 |
| G002 | E1–E3 and manifest are committed as a planning baseline |
| G003 | Planning baseline is published and local `main` equals `origin/main` |
| G004 | Worktree clean |
| G005 | Baseline `unittest` and `pytest` each pass 250/250 |
| G006 | P0/LOCKED file and tree identities recorded |
| G007 | E1–E3 hashes equal this manifest |
| G008 | PIN001–PIN004 equal this manifest |
| G009 | Published run `29565319116` environment evidence remains accessible or preserved |
| G010 | IM001–IM012 paths have no unexpected conflict |
| G011 | Docker is not required/running for execution |
| G012 | OpenAI and Local AI credentials/services are not required for default verification |
| G013 | Temporary clean-profile workspace available outside repository |
| G014 | Rollback and stop conditions acknowledged |

Any failure stops execution before branch creation.

## Implementation checkpoints

### CP1 — documentation contract

Required:

- W1 commit contains only IM001, IM002, and IM008 additions;
- T001–T011 and T020–T023 pass;
- E1–E3 hashes unchanged;
- no runtime/state/provider/protected diff;
- whitespace passes.

### CP2 — dependency profiles

Required:

- provisional W2 commit contains only IM003–IM005 and IM008 additions;
- T012–T015 pass;
- DEP001–DEP020 pass;
- independently validated fresh Ubuntu/CPython 3.14 default profile excludes
  OpenAI;
- independently validated fresh Ubuntu/CPython 3.14 optional profile includes
  OpenAI;
- no service/API request occurs;
- full regression passes in default profile;
- constraints content equals manifest exactly.

On PASS, the provisional commit becomes W2 without amendment. Record the Ubuntu
run identifier and logs for W4 evidence. On FAIL, do not proceed to W3.

### CP3 — CI parity and local completion

Required:

- W3 commit contains only IM006–IM008 additions;
- all T001–T024 pass;
- CI001–CI014 static controls pass;
- local `unittest` and pytest pass with expanded count;
- compile and whitespace pass;
- fresh-clone simulation passes;
- P0/LOCKED identities and E1–E3 hashes exact;
- branch diff equals IM001–IM008 only.

### CP4 — evidence ready for acceptance

Required:

- first pull-request workflow runs complete without retry or weakening;
- required and observational jobs pass;
- W4 contains only IM009–IM012;
- EVID001–EVID012 and REC001–REC004 pass;
- worktree clean;
- branch remains unmerged.

### CP5 — post-merge closeout

Only after Product Owner authorization:

- preserve W1–W4 commits with normal merge unless a different method is
  separately authorized;
- synchronize local `main` and `origin/main`;
- repeat default clean-profile install, focused tests, full regression,
  compilation, whitespace, constraints, pins, and protected identities;
- confirm `main` workflows pass;
- record final closeout in a separate scoped commit.

## Verification commands

Exact local commands:

```text
git status --short
git diff --check
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest \
  tests.test_f11e_environment_contract -v
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover tests
PYTHONDONTWRITEBYTECODE=1 python3 -m pytest
```

Compilation uses the existing F1.1-D active scope with
`PYTHONPYCACHEPREFIX` outside the repository.

Clean-profile Ubuntu commands:

```text
python -m pip install \
  -c constraints/cpython314-ubuntu.txt \
  -r requirements.txt
python -m pip check
python -m pip freeze

python -m pip install \
  -c constraints/cpython314-ubuntu.txt \
  -r requirements-openai.txt
python -m pip check
python -c "import openai; print(openai.__version__)"
```

The import check must run with OpenAI credential variables absent and network
requests prohibited.

Protected-scope verification:

```text
git diff --name-only <planning-baseline>...HEAD
git hash-object <recorded P0 files>
git rev-parse HEAD:<recorded P0 trees>
shasum -a 256 <E1-E3 artifacts>
```

## Generated and temporary artifact policy

Tracked generated content is limited to IM005. Temporary environments, pip
caches, freeze comparisons, logs, compiled bytecode, and fresh clones must be
outside the repository or ignored.

No generated artifact may contain:

- credentials or environment values;
- local absolute paths;
- provider responses;
- memory data;
- research/replay content;
- GitHub tokens;
- external capability output.

## Stop conditions

Stop immediately and preserve factual evidence if:

- any gate or checkpoint fails;
- dependency resolution differs from IM005;
- either clean profile fails;
- default profile installs OpenAI;
- a credential/service becomes necessary;
- a workflow pin or semantic boundary differs;
- tests require runtime/source modification;
- canonical verification changes tracked memory;
- an unlisted file appears;
- packaging or Docker provisioning appears necessary;
- P0/LOCKED or E1–E3 identities change;
- CI first run fails;
- implementation scope conflicts with current repository state.

Do not retry external workflow runs, modify expectations, alter versions, or
weaken controls without a new authorization.

## Rollback points

| Point | Rollback |
| --- | --- |
| R0 — before branch | No implementation exists; retain planning baseline |
| R1 — after W1 | Normal revert of W1; verify documentation baseline and hashes |
| R2 — provisional/official W2 | Preserve Ubuntu validation evidence; revert W2 as one unit if required; restore requirements/dependency state; repeat baseline install/tests |
| R3 — after W3 | Revert W3 then W2 as needed so workflow commands never reference missing constraints |
| R4 — after W4/pre-merge | Preserve PR/CI evidence; revert scoped commits or close branch without history rewrite |
| R5 — post-merge | Reviewed revert commits in reverse dependency order; never rewrite published history |

After any rollback:

- run baseline 250-test regression;
- verify requirements/workflow consistency;
- verify pins and protected identities;
- verify E1–E3 hashes;
- record the rollback outcome factually.

## Completion boundary

Manifest execution is complete only when IM001–IM012 and all applicable
controls pass, W1–W4 are preserved, and Product Owner acceptance is requested.

F1.1-E itself is not complete until an authorized merge, CP5 verification, and
separate closeout record succeed.

## Recommendation

**READY FOR PRODUCT OWNER MANIFEST REVIEW**

**IMPLEMENTATION NOT AUTHORIZED**
